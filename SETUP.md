# Быстрая настройка для GitHub Pages

## ⚠️ ВАЖНО: Перед деплоем выполните эти шаги!

### 1. Проверьте название вашего репозитория

Откройте `vite.config.ts` и убедитесь, что `base` соответствует названию вашего репозитория:

```ts
base: '/admin.panel/',  // замените admin.panel на название ВАШЕГО репозитория
```

**Примеры:**
- Если репозиторий `my-admin` → `base: '/my-admin/'`
- Если репозиторий `admin-panel` → `base: '/admin-panel/'`
- Если репозиторий в корне (username.github.io) → `base: '/'`

### 2. Обновите package.json

Замените `YOUR_USERNAME` на ваш GitHub username:

```json
"homepage": "https://YOUR_USERNAME.github.io/admin.panel"
```

### 3. Установите зависимости и задеплойте

```bash
npm install
npm run build
npm run deploy
```

### 4. Настройте GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. **Source**: выберите `gh-pages` branch
3. **Folder**: `/ (root)`
4. Сохраните

### 5. Подождите 1-2 минуты

GitHub обновит страницу. После этого ваш сайт должен работать!

## 🔍 Проверка

После деплоя откройте:
```
https://YOUR_USERNAME.github.io/admin.panel
```

Если видите белую страницу:
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в Console
3. Проверьте, что все файлы загружаются в Network tab
4. Убедитесь, что `base` в vite.config.ts правильный

## ✅ Что уже исправлено:

- ✅ Используется `HashRouter` вместо `BrowserRouter`
- ✅ Создан `404.html` для SPA
- ✅ Настроена базовая конфигурация


