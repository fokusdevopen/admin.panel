<<<<<<< HEAD
# Решение проблем: 404 и GitHub Actions

## ✅ Исправлено:

1. **GitHub Actions** - изменен с `npm ci` на `npm install` (не требует package-lock.json)
2. **Добавлена проверка сборки** - скрипт `check-build.js` проверяет правильность путей

## 🔧 Что нужно сделать:

### 1. Убедитесь что base path правильный

Откройте `vite.config.ts` и проверьте:
```ts
const base = '/admin.panel/'  // должно совпадать с названием репозитория!
```

**КРИТИЧЕСКИ ВАЖНО:** Название должно точно совпадать с названием вашего репозитория на GitHub!

### 2. Соберите проект локально:

```bash
npm run build
npm run check-build
```

Скрипт `check-build` покажет правильность путей в собранном `dist/index.html`.

### 3. Проверьте dist/index.html:

После сборки откройте `dist/index.html` и найдите строки вида:
```html
<script type="module" crossorigin src="/admin.panel/assets/index-xxxxx.js"></script>
```

**Важно:** Путь должен начинаться с `/admin.panel/` (или вашего base path)!

### 4. Если пути неправильные:

Проблема в том, что base path не совпадает с реальным репозиторием. 

**Решение:**
1. Узнайте точное название репозитория на GitHub
2. Обновите `base` в `vite.config.ts`
3. Пересоберите: `npm run build`

### 5. Задеплойте:

```bash
npm run deploy
```

Или используйте GitHub Actions (уже исправлен).

## 🐛 Если 404 остаётся:

### Диагностика:

1. **Откройте Network tab в DevTools (F12)**
2. **Посмотрите какой именно файл не загружается**
3. **Проверьте URL** - должен начинаться с вашего base path

### Примеры правильных URL:
- ✅ `https://username.github.io/admin.panel/assets/index-abc123.js`
- ❌ `https://username.github.io/assets/index-abc123.js` (нет base path)
- ❌ `https://username.github.io/src/main.tsx` (неправильный путь)

### Если URL неправильный:

1. Проверьте `dist/index.html` после сборки
2. Если пути там неправильные - проблема в `base` в `vite.config.ts`
3. Если пути в `dist/index.html` правильные, но браузер загружает неправильные - проблема в кеше или настройках GitHub Pages

## 📝 Чеклист:

- [ ] `base` в `vite.config.ts` совпадает с названием репозитория
- [ ] `homepage` в `package.json` правильный
- [ ] Выполнен `npm run build`
- [ ] `dist/index.html` содержит правильные пути (начинаются с base path)
- [ ] `dist/.nojekyll` существует
- [ ] GitHub Pages настроен на `gh-pages` branch
- [ ] Очищен кеш браузера (Ctrl+Shift+R)

## 💡 Альтернатива: Использовать относительные пути

Если base path не работает, можно попробовать относительные пути:

В `vite.config.ts`:
```ts
const base = './'
```

Но это может вызвать проблемы с HashRouter. Лучше использовать правильный base path.



=======
# Решение проблем: 404 и GitHub Actions

## ✅ Исправлено:

1. **GitHub Actions** - изменен с `npm ci` на `npm install` (не требует package-lock.json)
2. **Добавлена проверка сборки** - скрипт `check-build.js` проверяет правильность путей

## 🔧 Что нужно сделать:

### 1. Убедитесь что base path правильный

Откройте `vite.config.ts` и проверьте:
```ts
const base = '/admin.panel/'  // должно совпадать с названием репозитория!
```

**КРИТИЧЕСКИ ВАЖНО:** Название должно точно совпадать с названием вашего репозитория на GitHub!

### 2. Соберите проект локально:

```bash
npm run build
npm run check-build
```

Скрипт `check-build` покажет правильность путей в собранном `dist/index.html`.

### 3. Проверьте dist/index.html:

После сборки откройте `dist/index.html` и найдите строки вида:
```html
<script type="module" crossorigin src="/admin.panel/assets/index-xxxxx.js"></script>
```

**Важно:** Путь должен начинаться с `/admin.panel/` (или вашего base path)!

### 4. Если пути неправильные:

Проблема в том, что base path не совпадает с реальным репозиторием. 

**Решение:**
1. Узнайте точное название репозитория на GitHub
2. Обновите `base` в `vite.config.ts`
3. Пересоберите: `npm run build`

### 5. Задеплойте:

```bash
npm run deploy
```

Или используйте GitHub Actions (уже исправлен).

## 🐛 Если 404 остаётся:

### Диагностика:

1. **Откройте Network tab в DevTools (F12)**
2. **Посмотрите какой именно файл не загружается**
3. **Проверьте URL** - должен начинаться с вашего base path

### Примеры правильных URL:
- ✅ `https://username.github.io/admin.panel/assets/index-abc123.js`
- ❌ `https://username.github.io/assets/index-abc123.js` (нет base path)
- ❌ `https://username.github.io/src/main.tsx` (неправильный путь)

### Если URL неправильный:

1. Проверьте `dist/index.html` после сборки
2. Если пути там неправильные - проблема в `base` в `vite.config.ts`
3. Если пути в `dist/index.html` правильные, но браузер загружает неправильные - проблема в кеше или настройках GitHub Pages

## 📝 Чеклист:

- [ ] `base` в `vite.config.ts` совпадает с названием репозитория
- [ ] `homepage` в `package.json` правильный
- [ ] Выполнен `npm run build`
- [ ] `dist/index.html` содержит правильные пути (начинаются с base path)
- [ ] `dist/.nojekyll` существует
- [ ] GitHub Pages настроен на `gh-pages` branch
- [ ] Очищен кеш браузера (Ctrl+Shift+R)

## 💡 Альтернатива: Использовать относительные пути

Если base path не работает, можно попробовать относительные пути:

В `vite.config.ts`:
```ts
const base = './'
```

Но это может вызвать проблемы с HashRouter. Лучше использовать правильный base path.



>>>>>>> f126881 (Initial commit)
