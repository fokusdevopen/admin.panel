# Админ Панель - Цифровое Агентство

Полноценная веб-админ панель для управления цифровым агентством.

## 🚨 ВАЖНО: Если видите белую страницу на GitHub Pages

**Смотрите файл `QUICK_FIX.md` для быстрого решения!**

## 📋 Быстрый старт

### Локальная разработка

```bash
npm install
npm run dev
```

### Деплой на GitHub Pages

1. **Обновите `vite.config.ts`** - замените `'admin.panel'` на название вашего репозитория
2. **Обновите `package.json`** - замените `YOUR_USERNAME` на ваш GitHub username
3. **Выполните:**
```bash
npm install
npm run build
npm run deploy
```

4. **Настройте GitHub Pages:**
   - Settings → Pages
   - Source: `gh-pages` branch
   - Folder: `/ (root)`

## 🔧 Структура проекта

```
admin.panel/
├── src/
│   ├── components/     # Компоненты
│   ├── pages/         # Страницы
│   ├── App.tsx         # Главный компонент
│   └── main.tsx        # Точка входа
├── public/            # Статические файлы
└── dist/              # Собранный проект (после build)
```

## 📝 Документация

- `QUICK_FIX.md` - Быстрое решение проблемы белой страницы
- `TROUBLESHOOTING.md` - Подробная диагностика проблем
- `GIT_SETUP.md` - Настройка Git и деплоя
- `DEPLOY.md` - Инструкции по деплою

## 🐛 Решение проблем

### Белая страница на GitHub Pages

1. Проверьте, что `base` в `vite.config.ts` совпадает с названием репозитория
2. Проверьте, что `homepage` в `package.json` правильный
3. Пересоберите: `npm run build && npm run deploy`
4. Очистите кеш браузера (Ctrl+Shift+R)

### Ошибки в консоли

Откройте DevTools (F12) → Console и проверьте ошибки. Частые проблемы:
- Неправильные пути к файлам → проверьте `base` в vite.config.ts
- Модули не найдены → выполните `npm install`
- 404 ошибки → проверьте настройки GitHub Pages

## 📄 Лицензия

MIT
