# 🚨 БЫСТРОЕ РЕШЕНИЕ: Белая страница

## Выполните эти 3 шага:

### 1. Узнайте название вашего репозитория

Откройте ваш репозиторий на GitHub и посмотрите URL:
```
https://github.com/USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ
```

Скопируйте **НАЗВАНИЕ_РЕПОЗИТОРИЯ** (например: `admin.panel`, `my-admin`, и т.д.)

### 2. Обновите 2 файла

#### Файл 1: `vite.config.ts`

Найдите строку:
```ts
const base = '/admin.panel/'
```

Замените на:
```ts
const base = '/ВАШЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ/'
```

**Пример:** Если репозиторий `my-admin-panel`, то:
```ts
const base = '/my-admin-panel/'
```

#### Файл 2: `package.json`

Найдите строку:
```json
"homepage": "https://YOUR_USERNAME.github.io/admin.panel"
```

Замените `YOUR_USERNAME` на ваш GitHub username и `admin.panel` на название репозитория:
```json
"homepage": "https://ВАШ_USERNAME.github.io/ВАШЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ"
```

**Пример:** Если username `john` и репозиторий `my-admin`:
```json
"homepage": "https://john.github.io/my-admin"
```

### 3. Пересоберите и задеплойте

```bash
npm run build
npm run deploy
```

### 4. Проверьте GitHub Pages настройки

1. GitHub → ваш репозиторий → **Settings** → **Pages**
2. **Source**: `gh-pages` branch
3. **Folder**: `/ (root)`
4. Сохраните

### 5. Подождите и проверьте

- Подождите 1-2 минуты
- Откройте: `https://ВАШ_USERNAME.github.io/ВАШЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ`
- Нажмите **Ctrl+Shift+R** для жесткой перезагрузки (очистка кеша)

## ❓ Если всё ещё не работает:

1. Откройте консоль браузера (F12)
2. Посмотрите на ошибки в Console
3. Проверьте Network tab - какие файлы не загружаются (красные)
4. Убедитесь, что base path в vite.config.ts **ТОЧНО** совпадает с названием репозитория

## 💡 Важно:

- Название репозитория чувствительно к регистру! `AdminPanel` ≠ `adminpanel`
- После изменений ОБЯЗАТЕЛЬНО выполните `npm run build` перед `npm run deploy`
- Очистите кеш браузера (Ctrl+Shift+R)



