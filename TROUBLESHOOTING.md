# Решение проблемы: Белая страница на GitHub Pages

## 🔍 Диагностика

Если видите белую страницу, выполните эти шаги:

### 1. Проверьте консоль браузера

Откройте DevTools (F12) → Console и посмотрите на ошибки. Частые ошибки:

- `Failed to load resource` - неправильные пути к файлам
- `404 Not Found` - файлы не найдены
- `Cannot find module` - проблемы с импортами

### 2. Проверьте Network tab

В DevTools → Network посмотрите, какие файлы не загружаются (красные).

### 3. Проверьте настройки

## ✅ Пошаговое решение

### Шаг 1: Узнайте точное название вашего репозитория

Перейдите на GitHub и посмотрите URL вашего репозитория:
```
https://github.com/USERNAME/REPO_NAME
```

`REPO_NAME` - это название репозитория.

### Шаг 2: Обновите vite.config.ts

Откройте `vite.config.ts` и замените:

```ts
const base = '/ТОЧНОЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ/'
```

**Важно:** Название должно точно совпадать, включая регистр!

### Шаг 3: Пересоберите проект

```bash
npm run build
```

### Шаг 4: Проверьте dist/index.html

После сборки откройте `dist/index.html` и проверьте пути к скриптам. Они должны начинаться с вашего base path.

### Шаг 5: Задеплойте заново

```bash
npm run deploy
```

### Шаг 6: Очистите кеш браузера

После деплоя:
- Нажмите Ctrl+Shift+R (или Cmd+Shift+R на Mac) для жесткой перезагрузки
- Или откройте в режиме инкогнито

## 🔧 Альтернативное решение: Использовать корневой домен

Если у вас есть домен `username.github.io`, можно использовать корневой путь:

### В vite.config.ts:
```ts
const base = '/'
```

### В package.json:
```json
"homepage": "https://username.github.io"
```

## 🐛 Частые проблемы

### Проблема: "Cannot GET /admin.panel/"

**Решение:** Проверьте, что в GitHub Pages настроен правильный branch (`gh-pages`) и folder (`/ (root)`)

### Проблема: Файлы загружаются, но страница белая

**Решение:** 
1. Проверьте консоль на ошибки JavaScript
2. Убедитесь, что используется HashRouter (уже настроено)
3. Проверьте, что все компоненты импортированы правильно

### Проблема: Стили не применяются

**Решение:**
1. Убедитесь, что Tailwind CSS правильно настроен
2. Проверьте, что `index.css` импортирован в `main.tsx`

## 📞 Нужна помощь?

Если ничего не помогает:

1. Откройте консоль браузера (F12)
2. Скопируйте все ошибки
3. Проверьте Network tab - какие файлы не загружаются
4. Убедитесь, что base path в vite.config.ts точно совпадает с названием репозитория

